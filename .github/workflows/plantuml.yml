name: PlantUML Diagrams
on:
  push:
    branches:
      - master
    paths:
      - "diagrams/**"
  pull_request:
    branches:
      - master
    paths:
      - "diagrams/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-diagrams:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Install PlantUML
        run: |
          sudo apt-get update
          sudo apt-get install -y plantuml

      - name: Generate Diagrams
        run: |
          mkdir -p img
          for file in diagrams/*.puml; do
            if [ -f "$file" ]; then
              filename=$(basename "$file" .puml)
              # Генерируем во временную папку и переименовываем
              plantuml "$file" -o ../temp/
              # Находим созданный файл и переименовываем
              for generated in temp/*.png; do
                if [ -f "$generated" ]; then
                  mv "$generated" "img/$filename.png"
                fi
              done
            fi
          done
          # Удаляем временную папку
          rm -rf temp

      - name: Commit and push diagrams
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add img/ --force
          git commit -m "🤖 Auto-generate PlantUML diagrams [skip ci]" --allow-empty
          git push
